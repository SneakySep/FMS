# Freight-Management-System

Freight Management for Capstone.

A Priority Handling Logistics, Inc. freight-management demo consisting of three frontends:

- `Frontend-CustomerDash/` — Customer Portal
- `Frontend-SalesAgent/` — Sales Agent Portal
- `Frontend-LandingPage/` — Public Landing Page

> **Note:** This is currently a static frontend mockup (client-side data via `localStorage`).
> No database layer exists yet. When a backend is added, use the secure PDO bootstrap in
> `config/db.php` (prepared statements only) and keep credentials out of source control.
